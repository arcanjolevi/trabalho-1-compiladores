int main() {
  int x, a, b, d;
  int y;
  int aA;
  char a = 'a';
  int vet[123];

  if (1 > 2) {
    x = y;
  }

  for(x = 0; x < y; x = x + 1){
    vet[y] = 1;
  }

  while(2 > 3){
    x = y + 1;
  }

  return 0;
}

